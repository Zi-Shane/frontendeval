export type FormObj = { title: string; description: string; value: string };

export type updateFormFunc = (newFormAnswer: FormObj) => void;

// enum FormType {
//   text = "text",
//   options = "options",
// }
